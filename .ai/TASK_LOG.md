# homeweb Task Log

Keep this concise. Record outcomes and decisions, not full chat transcripts.

## 2026-06-26
- Generated Claude/context handoff files.
- Added `.ai/` documentation structure for project continuity.

## 2026-07-30
- Request: Move the Sprite Vault static backup site/mirror into the `homeweb` repo.
- Files changed: added `sprite-vault/`, `Tools/SpriteVaultMirror/`, and `.github/workflows/update-sprite-vault-mirror.yml`; updated `.ai/CURRENT_STATE.md`.
- Decisions: the public mirror URL is `https://misaellandero.com/sprite-vault/manifest.json`; the workflow regenerates data every 12 hours and commits only when the mirror changes.
- Verification: generated mirror locally, validated JSON files with `python3 -m json.tool`, and compiled the generator with `python3 -m py_compile`.
- Follow-ups: confirm the deployment serves `sprite-vault/manifest.json` publicly after commit/push.

## 2026-07-30
- Request: Document that GitHub Pages links must target concrete files, not folder routes.
- Files changed: updated `CLAUDE.md`, `.ai/CODING_RULES.md`, `.ai/CURRENT_STATE.md`, and `Tools/SpriteVaultMirror/README.md`.
- Decisions: public links should use exact file paths and casing, for example `/Apps/PetPal/index.html` or `/sprite-vault/manifest.json`.
- Verification: checked no current `Apps/Gustambo` folder links were present and removed stale `Mirror/sprite-vault` README paths.
- Follow-ups: use explicit file links when generating the pending board and App Store screenshot gallery.

## 2026-08-01
- Request: Add a manual HomeWeb control panel for Sprite Vault Tracker catalog source failover.
- Files changed: added `Apps/SpriteVaultControl/Index.html` and `sprite-vault/control.json`; updated `Tools/SpriteVaultMirror/generate_mirror.py`, `Tools/SpriteVaultMirror/README.md`, and `.ai/CURRENT_STATE.md`.
- Decisions: the control modes are `automatic`, `spritelocker`, and `homeweb`; the public panel URL is `https://misaellandero.com/Apps/SpriteVaultControl/Index.html`.
- Verification: validated generator with `python3 -m py_compile`; validated `control.json`, `manifest.json`, `sprites.json`, and `backups/index.json` with `python3 -m json.tool`; public GitHub Pages URLs still return 404 until published.
- Follow-ups: publish through GitHub Pages so `https://misaellandero.com/sprite-vault/control.json` is reachable.

## 2026-09-01
- Request: Reorder the main index page so the "Me" section/tab comes after "Portafolio".
- Files changed: `index.html` — swapped the order of the `#Portafolio` and `#SobreMi` sections, and reordered the nav bar links to match (Portafolio, Me, Instagram, Resume).
- Verification: confirmed `<section>`/`</section>` tag counts balanced (3/3), total line count unchanged (565), and nav `href` order matches new section order via grep.
- Follow-ups: none.

## 2026-09-01 (2)
- Request: Add floating app icons to the hero background (like the decorative squares in the "Get a cost proposal" section), and make all portfolio cards uniform — apps without an App Store link need a "Join the Waitlist"-style badge so every card keeps the same footer layout.
- Files changed: `index.html` — added a `<style>` block (`.hero-app-icons`/`.hero-app-icon` float animation, `.badge-store`/`.badge-store-pending` badge styles); added 6 floating app icons (Cota, DebtMe, Revisits, Loxi, Fox vs Hunters, Caffeinate Bar) absolutely positioned in `.page-header`, hidden below 992px; gave every portfolio card a two-row footer (store/status badge + "Go to the website"): added a "Join the Waitlist" badge to Cota and Fox vs Hunters, fixed Cota's link to use the explicit `index.html` file per the GitHub Pages linking rule, and fixed a stray duplicate quote in DebtMe's App Store `href`.
- Decisions: floating icons use low opacity (.14) and edge/corner positions to avoid overlapping the nav, hero text, and social buttons; status badges are styled as an outlined pill (not a fake store badge) to stay honest about release state while matching the visual weight of real App Store badges.
- Verification: rendered `index.html` locally with Playwright (Chromium) via `file://`, screenshotted the hero and portfolio sections, and visually confirmed icon placement/opacity and uniform card footers; pre-existing CORS console errors from `sistema/php/*` XHR calls are unrelated (no PHP backend under `file://`, present before this change too).
- Follow-ups: none.

## 2026-09-01 (3)
- Request: Portfolio cards still had uneven heights; the "Resume" nav link wasn't opening the resume page.
- Files changed: `index.html` — added `.section-coins .portfolio-grid > [class*="col-"] { display:flex; }` and `.section-coins .portfolio-grid .card.card-coin { width:100%; height:100%; }` (scoped to this page only) so each portfolio card stretches to match the tallest card in its row, keeping footers aligned regardless of description length; changed the Resume nav link from `href="resume/"` to `href="resume/index.html"`.
- Decisions: used Bootstrap's existing flex row/`.card-body{flex:1 1 auto}` machinery (equal-height-card pattern) rather than fixed min-heights, so it stays correct if copy changes later; the resume link needed the explicit file per the project's GitHub Pages linking rule, and directory-style links don't resolve under `file://` testing anyway.
- Verification: re-rendered locally with Playwright — confirmed all 3 cards in each portfolio row now share the same bottom edge, and that clicking the Resume link navigates to and correctly renders `resume/index.html`.
- Follow-ups: `resume/` is still untracked in git (`git status` shows `?? resume/`) — it must be added and pushed before the Resume link will work on the live `misaellandero.com` site, not just locally.

## 2026-09-16
- Request: On Cota's wish list, show how many people signed up for app news, and a breakdown by selected platform (iOS, Android, iOS beta, etc.). User asked for both a public counter on the wish list page and an admin panel.
- Files changed: `Apps/Cota/index.html` — added an "iOS Beta (TestFlight)" checkbox to the platform choice fieldset, and a public `.waitlist-stats` block (total + per-platform pill counts) under the form note; `Apps/Cota/assets/js/waitlist.js` — added `iOS Beta` to `PLATFORM_STAT_KEYS`, translations for the new checkbox label and stats total label, `renderStats`/`loadStats` to read and display `cotaWaitlistStats/summary`, and replaced the plain `setDoc` write with a `runTransaction` (`submitToWaitlist`) that diffs previously selected platforms against the new selection so the aggregate counters (`total`, `ios`, `web`, `android`, `iosBeta`) stay accurate across repeat submissions, preserves the original `submittedAt`, and refreshes the public counter after a successful signup; `Apps/Cota/assets/css/styles.css` — switched `.platform-choice` to an `auto-fit` grid (now 5 options) and added `.waitlist-stats`/`.stats-*` styling matching the existing dark glass theme; added `Apps/Cota/admin.html` + `Apps/Cota/assets/js/admin.js` — a Firebase Auth (email/password) gated dashboard that reads the full `cotaWaitlist` collection, recomputes total and per-platform counts live (self-healing, not dependent on the stats doc), and lists every entry (email, platforms, language, submitted date) in a table, with HTML-escaping on all rendered user-submitted fields.
- Decisions: kept the aggregate counts in a separate `cotaWaitlistStats/summary` document (no PII) instead of exposing the raw `cotaWaitlist` collection to public reads, so the public counter never needs "list" access to documents containing emails; admin panel re-derives counts directly from the raw collection (not the summary doc) so it self-corrects if the summary ever drifts; used Firebase Authentication for the admin gate (this project has no existing password/token-admin pattern for Firestore-backed data, unlike SpriteVaultControl's pasted-GitHub-token pattern, which doesn't apply to Firestore security rules).
- Verification: `node --check` passed on both new/changed JS files; HTML parses cleanly (Python `html.parser`) for `index.html` and `admin.html`; rendered both pages with Playwright/Chromium over a local `http.server` and screenshotted — layout, new "iOS beta" option, stats pills, and the admin login form all render correctly. Could NOT verify the actual Firebase read/write/transaction behavior or the admin dashboard's post-login data load — this sandbox has no outbound network access to `firestore.googleapis.com` / `identitytoolkit.googleapis.com` (Playwright showed `ERR_TUNNEL_CONNECTION_FAILED` loading the Firebase SDK), and there is no local Firebase emulator in this repo.
- Follow-ups: Firestore security rules for the `misaellanderoweb` project must be updated in the Firebase console (not managed as code in this repo) to support this feature: (1) `cotaWaitlist/{emailId}`: allow `get` (read by exact doc ID) in addition to the existing write, so the client can diff previous platform selections — but keep `list` denied so emails can't be enumerated; (2) `cotaWaitlistStats/summary`: allow public `get`/`read`, and allow `update`/`create` limited to numeric increments on the known fields (`total`, `ios`, `web`, `android`, `iosBeta`); (3) for `admin.html`/`admin.js` to work, an admin user must be created under Firebase Authentication → Email/Password, and `cotaWaitlist` must allow `list`/`get` when `request.auth != null` (ideally scoped further to that admin's UID or email). None of this could be applied or tested from this repo/sandbox.
- Pushed directly to `main` (fast-forward, user explicitly requested it to validate on the live site) at the user's request, without a PR.

## 2026-09-16 (2) — Hotfix
- Incident: user shared their live Firestore rules — `cotaWaitlist` had `allow read, delete: if false;` and there was no rule at all for `cotaWaitlistStats` (default-denied). The 2026-09-16 change wrapped the waitlist write in a `runTransaction` that reads the entry doc first; under `read: if false` that read was denied, which failed the *entire* transaction — breaking real Cota waitlist signups on `main`/production, not just the new stats feature (it silently fell back to the existing `localStorage` copy, so the UI still showed success).
- Files changed: `Apps/Cota/assets/js/waitlist.js` — replaced the single `runTransaction` in `submitToWaitlist` with: (1) `readPreviousEntry` — a best-effort `getDoc` wrapped in try/catch that returns `null` (not a thrown error) if the read is denied; (2) the core `setDoc` write to `cotaWaitlist`, which no longer depends on that read succeeding at all; (3) `updateStats` — a separate, fire-and-forget `setDoc(..., {merge:true})` with `increment()` deltas, only attempted when `readPreviousEntry` succeeded, with its own `.catch` so a permissions failure there can never block or fail the signup. Removed the now-unused `runTransaction` import (increment() is already an atomic server-side field transform without needing a transaction).
- Decisions: prioritized "signups always work" over "stats are always perfectly accurate" — if Firestore rules still deny reads, submissions succeed and stats simply stay unset until rules are updated (with a `console.warn`, not a user-facing error). This is the correct dependency direction for a primary-feature/secondary-feature split.
- Verification: `node --check` passed; re-read the full file to confirm control flow (write always attempted; stats update only on the non-blocking path). Same sandbox network limitation as before — could not exercise this against live Firestore.
- Follow-ups: same rule changes as the entry above are still needed for the public counter and admin panel to actually populate; gave the user the exact rules to paste (merged with their existing `cotaWaitlist` rule) — `allow get: if true`, `allow list: if request.auth != null`, plus a new `cotaWaitlistStats/summary` match block with public read/write. Pushed this fix directly to `main` (fast-forward) given the live regression and the user's earlier explicit direct-to-main request.

## 2026-09-16 (3)
- Request: after applying the Firestore rules and creating the admin user, the user reported the public counter on the landing page still didn't reflect entries that already existed in `cotaWaitlist` from before this feature shipped.
- Root cause: expected, not a bug — `cotaWaitlistStats/summary` is only ever updated incrementally by new submissions (see the 2026-09-16 hotfix); pre-existing `cotaWaitlist` documents were never counted into it, so the public counter started from whatever it was (0, or only post-fix signups) instead of the true total.
- Files changed: `Apps/Cota/assets/js/admin.js` — `loadEntries()` now also stores the counts it already computes from the full collection scan as `lastComputedStats` (`{total, ios, web, android, iosBeta}`); added `syncPublicStats()`, which overwrites `cotaWaitlistStats/summary` with those exact numbers (a full `setDoc`, not an increment, so it always matches the raw collection exactly); wired a new "Sincronizar contador público" button to it. `Apps/Cota/admin.html` — added that button next to "Actualizar"/"Cerrar sesión".
- Decisions: kept this as an authenticated-admin-only action (not automatic) since a full collection scan + overwrite is heavier than the per-signup increment and doesn't need to run on every page load; reused the counts already computed for the dashboard table instead of a second query.
- Verification: `node --check` passed; rendered `admin.html` with Playwright (forced the dashboard section visible without auth, since Firebase itself is unreachable from this sandbox) and confirmed the new button renders correctly alongside the existing ones.
- Follow-ups: user needs to log into `Apps/Cota/admin.html` and click "Sincronizar contador público" once to backfill the counter with all pre-existing signups; from then on new signups keep it accurate automatically.

## 2026-09-20
- Request: On the Revisits landing page, add a "web version" button; on Windows/Android show PWA install instructions, on Apple devices redirect to the existing App Store link.
- Files changed: `Apps/Revisits/index.html` — added a "Usar en la Web" button next to the App Store CTA, plus a Bootstrap 4 modal (`#webInstallModal`) for install instructions and a script include; `Apps/Revisits/assets/js/web-install.js` — new file, detects Apple (iOS/iPadOS/macOS) vs. Android vs. Windows vs. other via `navigator.userAgent`/`navigator.platform`, redirects Apple users straight to the App Store link, and otherwise fills the modal with platform-specific install steps and a link to `web/index.html`; `Apps/Revisits/web/` — new minimal PWA scaffold: `index.html` (branded "coming soon" shell, registers the service worker), `manifest.json` (name, icons, `standalone` display, brand theme color `#235ee7`), `sw.js` (basic cache-first app-shell service worker), `icons/icon-192.png` and `icons/icon-512.png` (generated from the existing `assets/images/iconiOS26.png` via Pillow, installed as a one-off local tool, not a repo dependency).
- Decisions: kept `Apps/Revisits/web/` as a placeholder shell (no real revisits/local-storage functionality yet, per user's explicit choice) so the new button has a real, installable destination instead of a dead link; iPadOS detection uses the `navigator.platform === 'MacIntel' && maxTouchPoints > 1` heuristic since modern iPadOS reports as Mac in the UA string.
- Verification: `python3 -m json.tool` passed for `manifest.json`; both HTML files parse cleanly with Python's `html.parser`; `node -c` passed for `web-install.js` and `sw.js`. Not verified: actual browser install prompts (`beforeinstallprompt`) and real device behavior on iOS/Android/Windows, since this sandbox has no way to launch those OSes/browsers — the user should manually confirm the modal text and the Apple redirect on a real device.
- Follow-ups: when the real Revisits Web app (with the IndexedDB-backed local storage discussed earlier) is built, replace the placeholder content in `Apps/Revisits/web/index.html` and expand `sw.js`'s cached asset list accordingly.

## 2026-09-20 (2)
- Request: "Implement the complete app" — port as many features as feasible from the native `revisits` Xcode app (Swift/SwiftUI/Core Data) into the `Apps/Revisits/web/` PWA placeholder, and port the native app's art/assets too.
- Research: spawned an Explore subagent against `/home/user/revisits` to map the Core Data schema (`Territory`, `Revisit`, `Visit`, `Service`, `DayGoal`, `Reports`, `Medals`), the exact form fields/labels/pickers in `RevisitMultiPlataformForm.swift`/`NewVisitMultiPlataformFormData.swift`/`NewReportMultiPlataformFormData.swift`, the circular-timer behavior (`CircularTimerView.swift`), the medal-earning logic (`GoalCelebrationView.swift` — 4 medal types tied to day/week/month/year goal cadence, NOT hour-rank badges like "Auxiliar/Precursor" as initially assumed), Spanish/English copy from `Shared/Localizable.xcstrings`, and exact asset paths (medals, house illustrations, SF Symbol exports, app icon).
- Files changed (all new except noted): `Apps/Revisits/web/js/db.js` (raw IndexedDB wrapper, 7 object stores mirroring the Core Data schema, generic CRUD + export/import), `js/utils.js`, `js/constants.js` (ported `PeopleTypes`/`PeopleEmoji`, visit-source picker options, medal image map), `js/settings.js` (localStorage-backed flags + timer state, standing in for `@AppStorage`), `js/notifications.js` (Notification API wrapper — due-reminder + timer-goal alerts), `js/ui.js` (modal/toast helpers), `js/store.js` (domain operations + `computeAndAwardMedals`, porting the native day→week→month→year goal-vs-actual priority logic), `js/views/revisits.js`, `js/views/reports.js`, `js/views/goals.js`, `js/views/settingsView.js` (the four tabs — see `.ai/CURRENT_STATE.md` for the full feature list), `js/app.js` (tab shell/router), `css/app.css` (new design system, brand gradient `#235ee7→#4ae7fa`). Rewrote `Apps/Revisits/web/index.html` (was the "coming soon" placeholder from the previous session, now the real app shell) and bumped `Apps/Revisits/web/sw.js` to `revisits-web-v2` precaching the full local app shell + art. Ported assets: `assets/art/medals/medal_{day,week,month,year}.png` (downscaled 1024²→128² with Pillow, one-off local tool, not a repo dependency) and `assets/art/houses/casa_{estudio,revisita}_1-6.png` (copied @2x from the native asset catalog, ~7-9KB each).
- Decisions: kept the whole app as vanilla ES modules loaded via `<script type="module">` — no bundler, no framework, no external JS dependency, consistent with the rest of `Apps/` in this repo and the "no dependencies without approval" rule; used plain IndexedDB instead of a wrapper library for the same reason. Dropped the native `Estudio.symbolset`/`Revisita.symbolset` SVGs after inspecting them — they're raw Adobe Illustrator/SF-Symbol-template exports with guide layers and a white "artboard" background rect, not renderable standalone in a browser `<img>`; used Font Awesome (already loaded elsewhere in this repo) instead rather than ship a broken asset. Simplified map/geolocation and PencilKit hand-drawing notes out of scope (no free maps API wired up, no canvas drawing UI) — documented as a known gap rather than half-implemented. Single Spanish locale (the app's own source language per `Shared/Localizable.xcstrings`) instead of porting all ~16 languages.
- Verification: `node --check` passed on all 12 JS modules and `sw.js`; `python3 -m json.tool` passed on `manifest.json`; both HTML files parse cleanly with Python's `html.parser`. Ran the app end-to-end with Playwright/Chromium (`/opt/pw-browsers/chromium`) over a local `python3 -m http.server`, using a persistent browser profile to also verify reload persistence and the service worker: created a service with a day-goal, created a revisit with an initial visit + a follow-up "Nueva visita" (confirmed the dynamic Biblia/Video/Publicación reference label updates), logged a manual report that met the day's goal and confirmed a medal was awarded and rendered, checked the Metas month calendar (32 cells incl. leading blanks) and progress bar, started/observed the timer, deleted a revisit, and exported a JSON backup (correct filename, download fires) — zero console/page errors across all runs (the only network errors seen were the sandbox's own proxy blocking the Font Awesome CDN, unrelated to app code). Not verified: real push notifications with the app fully closed, actual install-to-homescreen `beforeinstallprompt` flow, and any behavior on real iOS/Android/Windows devices — same sandbox limitation as the previous session.
- Follow-ups: see the new `Apps/Revisits/web/` bullet and the `Known Issues` entry in `.ai/CURRENT_STATE.md` for the explicit scope cuts (no maps, no drawing notes, no cloud sync, single locale, no legacy 70h fallback goal). If real background push and multi-device sync are wanted later, that requires a backend (e.g. Firebase, following the `Apps/Cota/` pattern already in this repo) — out of scope for a purely local-storage PWA and would need explicit approval per this repo's dependency/services rule.

## 2026-09-20 (3)
- Request: "Geolocation, yes implement it, and support for all languages" — a follow-up on the Revisits Web PWA (previous entry) asking for the two features explicitly scoped out earlier: real geolocation, and full multi-language support matching the native app.
- Files changed: new `Apps/Revisits/web/js/geo.js` (Geolocation API wrapper, Nominatim reverse-geocode, OSM embed/Google Maps directions URL builders); new `Apps/Revisits/web/js/i18n.js` (language state, `t()`/`personTypeLabel()`/`sourceLabel()`/`sourceRefLabel()`/`weekdayName()`/`weekdayShortName()`/`medalLabel()`/`visitTypeLabel()`, `initLanguage`/`getLanguage`/`setLanguage`/`onLanguageChange`); new `Apps/Revisits/web/js/i18n-strings.js` (the full `DICT` — 148 string keys + personTypes/sources/sourceRefs/weekdays/weekdaysShort/medals arrays — for all 18 languages the native app ships: es, en, de, fr, it, pt, nl, el, pl, ru, tr, uk, vi, th, id, hi, ja, ko). Updated `js/settings.js` (added `language` persisted setting), `js/constants.js` (removed the now-translated arrays, kept language-independent bits: `PEOPLE_EMOJI`, `SOURCES_EMOJI`, `VISIT_TYPES` internal enum, added `MEDAL_IMAGES`), `js/utils.js` (date formatters default to `i18n.getLocale()` instead of a hardcoded `es-MX`), `js/app.js` (calls `initLanguage(settings.language)` on boot, re-renders the whole shell + active tab on language change, translates tab labels/FAB aria-label/site link, sets `document.documentElement.lang`), and all four `js/views/*.js` files (every hardcoded Spanish string replaced with `t('key')` or a label helper; `revisits.js` also gained a "Ubicación" field in the add/edit form — "Usar mi ubicación actual"/"Actualizar ubicación" button, hidden `mapLat`/`mapLng` inputs, best-effort reverse-geocode into "Referencias de la casa" when empty — and the detail view now shows an embedded OpenStreetMap iframe + "Ver en el mapa"/"Cómo llegar" links when a revisit has a saved location). Bumped `sw.js` to `revisits-web-v3` to precache the 3 new JS files.
- Decisions: kept stored data values untranslated (`revisit.visitType` stays the literal string `'Estudio'`/`'Revisita'`, `personType`/`toolboxPub` stay numeric indexes, medal `type` stays `'day'|'week'|'month'|'year'`) so existing IndexedDB records and equality-based filters (`=== 'Estudio'`) keep working after a language switch — only display labels go through the translation layer. For maps, used the free OpenStreetMap `embed.html` iframe (no API key, no JS mapping library/dependency) for the preview and a plain Google Maps search URL for turn-by-turn directions, rather than adding a mapping SDK; reverse geocoding uses OSM's free Nominatim endpoint directly from the browser (no key), wrapped in try/catch so a failed/blocked lookup never blocks saving the revisit. For translations, reused the native app's own strings from `Shared/Localizable.xcstrings` wherever an exact match existed (English and the 7 other languages with String Catalog coverage: de/fr/it/pt/nl/pl/ru/tr, per the Explore-agent survey from the previous session), and translated everything else — all of el/uk/vi/th/id/hi/ja/ko plus new web-only strings in every language — directly, since this app's UI text doesn't have a 1:1 mapping to native app screens.
- Verification: `node --check` passed on all 20 JS files (including the two new i18n files); a script confirmed all 18 `DICT` entries have the full 148-key `strings` object plus complete 14/3/3/7/7-length arrays and all 4 medal keys (no missing keys in any language). Ran the app with Playwright/Chromium (mocked geolocation permission + coordinates) over a local server: captured a location on a new revisit, confirmed the status text and the "casa"/map preview appeared on the detail view, confirmed the "Cómo llegar"/directions link resolves to the correct `google.com/maps` URL with the captured coordinates, confirmed removing a saved location clears it and hides the map; separately verified language switching (default auto-detect from browser locale — tested both `en-US`→English and `es-MX`→Spanish, then manually switching to Japanese) re-renders tab labels, `<html lang>`, and list content correctly and persists across a reload; re-ran the full service/goal/report/medal regression flow from the previous session post-refactor with no change in behavior — zero unexpected console/page errors in any run (the only network errors were this sandbox's proxy blocking `nominatim.openstreetmap.org`, same class of restriction as the earlier Font Awesome CDN block, confirming the reverse-geocode failure path degrades gracefully rather than crashing).
- Follow-ups: the 8 languages with no native-app string catalog coverage (el, uk, vi, th, id, hi, ja, ko) — and any newer web-only string in any language — should ideally get a native-speaker review before this is treated as production-quality localization; flagged in `.ai/CURRENT_STATE.md`. Nominatim reverse geocoding has no API key/custom User-Agent (browsers can't set one) — acceptable for personal use per its usage policy, but would need a real geocoding backend if usage ever scales up.

## 2026-09-20 (4)
- Request: "I see a mix of English and Spanish, we should support automatic detection" — reported after the previous i18n work was merged to `main`.
- Root cause: the mix wasn't in the PWA (`Apps/Revisits/web/`, which already auto-detects across 18 languages) — it was on the marketing landing page (`Apps/Revisits/index.html`, `lang="en"`, all-English original copy, correctly left untouched per this repo's "preserve product copy" rule). The "Use on the Web" button and its install-instructions modal, added in an earlier session, were hardcoded in Spanish, sticking out against the rest of the English page.
- Files changed: `Apps/Revisits/assets/js/web-install.js` — added a self-contained `STRINGS` table (button label, modal title, "Open Revisits Web"/"Close" button text, and the android/windows/other instruction step arrays) for the same 18 languages as the PWA, plus `detectLanguage()` (checks `navigator.languages` in order, falls back to `'en'` — this page's own language — not `'es'`); on `DOMContentLoaded` it now sets the button/modal/link/close-button text before any click, in addition to driving the existing Apple-redirect and per-platform-instructions logic. `Apps/Revisits/index.html` — changed the static fallback copy for the button, modal title, and the two modal footer controls from Spanish to English (to match the page's own default language before JS runs) and added `id="openRevisitsWebLink"`/`id="webInstallCloseBtn"` so the script can localize them.
- Decisions: kept this as a separate, self-contained translation table in `web-install.js` rather than sharing `Apps/Revisits/web/js/i18n.js` — the landing page loads plain classic `<script>` tags (not ES modules) and is a different, smaller surface (~7 strings vs. the PWA's 148), so a lightweight independent table was simpler and avoided any module-loading changes to a page this repo's rules treat as fixed copy. Default fallback language is English (matching this page's actual `lang="en"` and copy), unlike the PWA which defaults to Spanish (its source language) — each surface falls back to its own native language.
- Verification: `node --check` passed; a script parsed the new `STRINGS` object and confirmed all 18 language keys have the required `btnLabel`/`modalTitle`/`openApp`/`close`/`android`/`windows`/`other` fields with non-empty instruction arrays. Also re-ran the earlier cross-check of every `t('key')` call in the PWA against `i18n-strings.js` (141 used keys, all present, no orphans) to confirm the previous session's i18n coverage was already complete and the reported mix was isolated to this landing-page script. Verified with Playwright/Chromium across `es-MX`, `en-US`, `ja-JP`, and `de-DE` browser locales on a local server: button label, modal title, install-step body text, and both footer buttons all switch together correctly per locale — no more mixed-language state. (Console output showed mojibake for non-Latin/accented text, traced to the sandbox terminal's own encoding when printing Playwright's `textContent()` results — confirmed the actual file bytes are correct UTF-8 and Playwright's in-page text extraction was correct; this was a terminal display artifact only, not a real bug.)
- Follow-ups: none outstanding for this fix. The broader translation-quality caveat from the previous entry still applies here too (el/uk/vi/th/id/hi/ja/ko instructions were translated directly by the assistant, not reviewed by a native speaker).

## 2026-09-20 (5)
- Request: "Disable the Apple redirect so I can test [the web app] and see what the iOS interface is like" — shared native-app screenshots (Metas/Reportes/Agenda/Territorios) for context. The App Store redirect added earlier was blocking the user from actually opening `Apps/Revisits/web/` on their own iPhone.
- Files changed: `Apps/Revisits/assets/js/web-install.js` — replaced `APP_STORE_URL_LANG` with a `APPLE_REDIRECT_ENABLED = false` flag (comment marks it as temporary/testing); when `false` (current state), clicking the button on an Apple device now navigates straight to `web/index.html` instead of the App Store link, skipping the install-instructions modal entirely so the user lands directly in the PWA.
- Decisions: kept the original App Store redirect code path intact behind the flag rather than deleting it, since it's the correct behavior for real end users and this is explicitly a temporary testing measure — flip `APPLE_REDIRECT_ENABLED` back to `true` (and the platform-specific direct-navigation branch can be left in place harmlessly, or removed) once testing is done.
- Verification: `node --check` passed; Playwright with an iPhone Safari user agent confirmed clicking the button now navigates to `web/index.html` rather than `apps.apple.com`.
- Follow-ups: **remember to re-enable `APPLE_REDIRECT_ENABLED = true` in `assets/js/web-install.js` once the user is done testing on iOS**, so real visitors on iPhone/iPad/Mac go back to the App Store instead of the web app.

## 2026-09-20 (6)
- Request: after testing the PWA on their Android phone (screenshots shared, compared against the native app's Territorios/Metas/Reportes/Ajustes screens), the user asked for a difference analysis, then: match feature parity with the native app, restyle closer to Android/Material Design, and specifically called out: tabs aren't 1:1 with native, missing tab icons/images, Informes no longer shows Publicaciones/Videos, and no install button/tutorial inside the app itself. Asked via `AskUserQuestion` on the two open architecture calls (Territorios map approach; dark theme depth) — user chose a real Leaflet+OpenStreetMap map (approving the PWA's first external JS dependency) and "follow system theme only" (no manual dark-mode toggle).
- Files changed: new `Apps/Revisits/web/js/views/territorios.js` (5th tab, Leaflet map with colored markers per revisit, popup links into `revisits.js`'s now-exported `openDetail`, graceful fallback message if Leaflet fails to load); new `Apps/Revisits/web/js/install.js` (`beforeinstallprompt` capture, dismissible bottom banner, `triggerInstall`/`isStandalone`/`canPromptInstall` helpers). Updated `index.html` (Leaflet CSS/JS from jsDelivr, no SRI — see Known Issues), `js/app.js` (5-tab order matching native: Territorios/Revisitas/Informes/Metas/Ajustes; `initInstallPrompt()` wired into bootstrap), `js/views/settingsView.js` (new "Instalar aplicación" card at the top), `js/views/reports.js` (Historial rows now include Publicaciones/Videos counts — the reported bug), `js/views/revisits.js` (exported `openDetail`; fixed the search input's hardcoded missing dark-mode background). Added 4 new i18n keys × 18 languages (`tabTerritorios`, `emptyTerritorios`, `mapPopupOpen`, `mapUnavailable`) and 8 new keys × 18 languages for the install UI (`installBannerTitle/Hint`, `btnInstallNow/NotNow`, `headingInstall`, `btnInstallApp`, `alreadyInstalledLabel`, `manualInstallHint`) to `js/i18n-strings.js`, injected via a one-off Node script (not committed) rather than 18 manual edits per key batch. Rewrote large parts of `css/app.css`: dark-mode custom properties, elevation tokens, Material-style filter chips (`.segmented` was a single shared track, now individual pill buttons), a Material 3-style active-tab pill indicator, `:active` press feedback, `.territorios-map`/`.install-banner`/`body.has-install-banner` layout, and — found during visual QA — fixed `.modal-sheet`'s hardcoded `background: #fff` (would have made every form/detail sheet's text unreadable in dark mode) plus the language `<select>`'s missing dark-mode background. Bumped `sw.js` to `revisits-web-v4`.
- Bug found and fixed independently of the request: 8 icon classes across the app (`fa-gear`, `fa-calendar-days`, `fa-map-location-dot`, `fa-location-crosshairs`, `fa-diamond-turn-right`, `fa-rotate-left`, `fa-triangle-exclamation`, `fa-xmark`) are Font Awesome **6** names, but this project loads Font Awesome **5.6.3** — those icons don't exist under those names in FA5, so they were almost certainly invisible since first added. Renamed to real FA5 equivalents (see `.ai/CURRENT_STATE.md` for the full mapping). Could not visually confirm in this sandbox (`use.fontawesome.com` is also network-blocked here); verified by cross-checking against the known FA5.6.3 icon set instead.
- Decisions: no SRI (`integrity`) hash on the Leaflet `<link>`/`<script>` tags — attempted to compute the real hash by downloading the files, but this sandbox's network policy blocks `cdn.jsdelivr.net` entirely (confirmed via `curl` and via a Playwright request-log check), so a hash could only have been guessed; a wrong guessed hash makes the browser silently refuse the resource, which would break the map for every real user — worse than shipping without SRI on a well-known, widely-audited CDN. Leaflet/map tiles are deliberately left out of `sw.js`'s precache list (same reasoning as Font Awesome: a cross-origin resource in `cache.addAll` risks failing the entire service-worker install), so Territorios needs connectivity and shows a translated fallback message otherwise instead of erroring. Kept dark mode purely to `prefers-color-scheme` per the user's explicit choice — no manual toggle UI was built.
- Verification: `node --check` passed on all JS files including the two new modules; a script confirmed all 18 languages have every new key with no gaps. Full Playwright regression pass (fresh profile, `es-MX` locale): confirmed the 5-tab order and labels, Territorios shows the graceful "couldn't load the map" fallback (Leaflet itself is blocked in this sandbox — the fallback path is what's actually exercised here, not the live map), a report with Publicaciones/Videos now displays both in Historial, the Ajustes "Instalar aplicación" card correctly showed the real "Instalar app" button (Chromium in this sandbox DID fire a genuine `beforeinstallprompt`, validating the whole flow against real browser behavior, not a mock), the install banner does appear on a fresh profile, and switching to English relabels all 5 tabs including the new "Territories". Also screenshotted light and dark mode at phone width (390×844): caught the `.modal-sheet` white-background-in-dark-mode bug this way (a "New Return Visit" form was rendered with barely-readable light-gray-on-near-white text before the fix; confirmed fully readable after). Separately caught and fixed a real layout bug during testing: the install banner's default position (`bottom: 76px`) overlapped normal card content (a "Nuevo" button in Metas got blocked from clicks) and also the FAB — fixed by moving the banner to `bottom: 148px` (clear of the FAB) and adding `body.has-install-banner { padding-bottom: 220px }` so page content doesn't sit underneath it. Could not verify: the live Leaflet map actually rendering tiles/markers (network-blocked here), Font Awesome icons actually painting (same block), and anything on a real Android/iOS device — the user should confirm both on their phone.
- Follow-ups: confirm on a real device that (1) the Territorios map loads and shows markers for revisits with saved locations, (2) all 5 tab bar icons render (not blank boxes) — this is the one part of today's fix that couldn't be visually verified at all in this sandbox, (3) the install banner/Ajustes install button behave as expected on Android Chrome. If icons still look wrong on-device despite the FA5/FA6 name fix, double check that `use.fontawesome.com/releases/v5.6.3/css/all.css` is actually the version being served (a stale cached service worker response could also mask the fix until the new `revisits-web-v4` cache activates).

## 2026-09-20 (7)
- Request: user confirmed (with a screenshot) that all 5 tab bar icons now render correctly on their Android phone after the FA5/FA6 icon-name fix from the previous entry. Then, comparing a PWA screenshot (Informes/Timer, confusing) against a native-app reference screenshot: "make the timer controls a big Ignore/Pause/Register button, and for empty lists add an explanation that there are no records yet plus a button to add one."
- Files changed: `js/views/reports.js` — reworked the timer: persisted state gained `running` (bool) and `accumulatedMs` (number) alongside the existing `startTime`/`presetSeconds`/`serviceId`/`counters`; added module-level `elapsedMs(state)` helper (returns `accumulatedMs` alone when paused, or `accumulatedMs + (now - startTime)` when running) used by both the live tick display and the "Registrar" hours prefill; the running-state controls are now three big buttons — **Ignorar** (`.btn-danger`, replaces the old small "Reiniciar" text button, same discard behavior/confirm dialog), **Pausar**/**Reanudar** (new — toggles `running`, freezes/resumes the tick `setInterval`), **Registrar** (unchanged behavior). Also gave Informes' empty Historial state an embedded "Registrar" button (previously only a header button existed, disconnected from the empty-state text). `js/views/goals.js` — empty services list now has an embedded "Nuevo" button (`openServiceForm()`). `js/views/revisits.js` — empty revisits list now has an embedded "Agregar revisita" button (`openNewRevisitForm()`); exported `openNewRevisitForm` is unchanged, just newly wired here too. `js/views/territorios.js` — empty (no saved locations) state now has an "Agregar revisita" button that switches to the Revisitas tab (via a synthetic click on its tab-bar button — no shared router exists between views) and opens the new-revisit form. `css/app.css` — `.empty-state` is now `display:flex; flex-direction:column; gap:14px` so an icon/text/button stack vertically with consistent spacing. Added 5 new i18n keys (`btnPause`, `btnResume`, `btnIgnore`, `confirmIgnoreTimer`, `btnAddFirstRevisit`) × 18 languages to `js/i18n-strings.js`.
- Decisions: kept backward compatibility for any already-persisted timer state missing the new fields by treating `running !== false` as "yes" and `accumulatedMs || 0` as the default — no explicit migration code needed since this is a brand-new feature with no real users yet, but it costs nothing to be defensive. Left the visit-history empty state (inside a revisit's detail sheet) and Medals' empty state without an added button: the former already sits directly under a prominent "Nueva visita" button in the same sheet (would be redundant), and medals are earned automatically, not something a user directly "adds".
- Verification: `node --check` passed on all touched files; a script confirmed the 5 new keys exist with no gaps across all 18 languages. Playwright pass (`es-MX` locale, dark mode): confirmed the timer freezes its displayed time exactly while paused (two reads 1s apart were identical) and resumes ticking correctly after; confirmed "Ignorar" returns to the preset-selection screen; confirmed each new empty-state button opens the right form (revisits, goals) — Territorios' button couldn't be exercised in this sandbox because Leaflet itself can't load here (network-blocked), so that view always falls into its "map unavailable" branch before ever reaching the empty/located branches; this is an environment limitation carried over from the previous entry, not a defect in the new code. Screenshotted the running timer at phone width in dark mode to confirm the three-button layout reads clearly (Ignorar/Pausar/Registrar, red/neutral/blue).
- Follow-ups: ask the user to confirm the Territorios "Agregar revisita" empty-state button (untestable here) actually switches tabs and opens the form correctly on their phone, where Leaflet can load.

## 2026-09-20 (8)
- Request: user asked what the "Ignorar" button was for, then said it's a bad translation and to check the native iOS app's UI and use its actual strings and component logic instead.
- Files changed: `js/views/reports.js` — renamed the timer's discard button back from "Ignorar"/`#ignoreBtn` to "Reiniciar"/`#restartBtn`, dropped the `confirm()` dialog before it fires entirely, and changed its icon from `fa-trash` to `fa-redo`. `js/i18n-strings.js` — removed the now-unused `btnIgnore`, `confirmIgnoreTimer`, and `confirmRestartTimer` keys (18 languages each) rather than leaving dead translations in the file; reused the pre-existing `btnRestart` key (already correctly translated in all 18 languages from before the "Ignorar" rename) for the button label.
- Decisions: read `Shared/Views/Reports/CircularTimer/CircularTimerView.swift` in the native `revisits` repo directly rather than relying on memory — confirmed the real button says "Reiniciar" (Spanish source string) with SF Symbol `arrow.clockwise.circle.fill`, calls `restart()` immediately on tap with **no** confirmation dialog anywhere near it (the two `.confirmationDialog`s in that file are for the watchOS Estudios +1/-1 picker and the service-type picker, unrelated), and isn't styled as a destructive/red button — it uses the same accent color as every other button. Matched all three: real string, closest available icon (`fa-redo`, a clockwise circular arrow — FA5 doesn't have the exact SF Symbol equivalent), and dropped both the confirmation prompt and the red danger styling to match the native app's actual (no-confirmation, non-alarming) interaction pattern, even though it's an irreversible action.
- Verification: `node --check` passed; a script re-confirmed every `t()` call used across the app still resolves in all 18 languages after removing the three dead keys (154 keys checked, zero gaps). Playwright: confirmed the button now reads "Reiniciar" and clicking it returns to the preset-selection screen immediately with no dialog intercepted (a page dialog listener that would have logged one saw nothing).
- Follow-ups: none.

## 2026-09-20 (9)
- Request: earlier asked "how could we use Material 3 Expressive in our app?" — answered as an exploratory question with a recommendation (hand-rolled CSS tokens over adding Google's `@material/web` library) and the tradeoff, without implementing. User replied "Implementa nuestra versión: tokens + CSS a mano" — i.e., build it.
- Files changed: `css/app.css` only (pure visual/token layer, no HTML/JS changes — same class names throughout, so risk was limited to visual regressions, not functional ones). Added to `:root`: an M3-style color-role system (`--md-primary/secondary/tertiary/error` plus a `-container`/`-on-container` pair for each, computed with `color-mix()` from the existing brand hues blended into the current `--surface`/`--text` — this makes every container tone dark-mode-aware automatically, no second hand-picked dark palette needed); a shape scale (`--shape-xs` through `--shape-xl`, `--shape-full`, and a one-off `--shape-fab`); a type scale (`--type-headline`, `--type-title-lg`/`-sm`, `--type-label-lg`/`-sm`, `--type-display`) used with the `font` shorthand; and `--easing-spring` (`cubic-bezier(0.34, 1.56, 0.64, 1)`, a slight overshoot curve). Applied these across existing rules: `.app-header h1`/`.card h2`/`.modal-sheet .modal-title h2` now use the type scale instead of ad hoc `font-size`/`font-weight`; `.card`/`.field input,select,textarea`/`.calendar-day`/badges/buttons/toast now pull their `border-radius` from the shape scale instead of hardcoded px or `999px`; `.stat-tile` background switched to `--md-surface-variant`, its value text and `.timer-display` sized up via `--type-display` (a bold 46px display size — the timer readout is now much more prominent); `.badge.upcoming` recolored to the tertiary container pair (was a flat gray); `.tabbar button.active i` and `.fab` now morph shape on activation/press (pill↔rounded-square, squircle↔circle) using `--easing-spring`; `.modal-sheet` gained a slide-up-with-overshoot entrance `@keyframes` animation.
- Bug fix found while touching this code: `.calendar-day.goal-met`/`.goal-missed` (Metas month calendar) had hardcoded light-mode colors (`#1c6b31` on `rgba(40,167,69,0.18)`) that would read as low-contrast in dark mode — replaced with `color-mix()` blends against the current `--surface`/`--text` so both states stay legible in either theme, consistent with the new token approach.
- Decisions: kept everything as plain custom properties + `color-mix()`/keyframes — no new library, matching the user's explicit choice and this repo's "no new dependencies without approval" default. The FAB needed its own `--shape-fab` (22px) rather than reusing `--shape-xl` (28px): on the FAB's 60px box, 28px reads as a near-circle (radius ≈ half the box), so a dedicated smaller value was needed for the "squircle" to actually look distinct from a circle — caught this via a screenshot, not by inspection alone.
- Verification: confirmed CSS brace balance (94/94) after all edits. Screenshotted phone-width light and dark mode for the Revisitas home screen, the running timer, and an open form modal — visually confirmed the bolder headline/timer type, the tonal chip/badge colors, the now-clearly-squircle FAB (before the `--shape-fab` fix it looked like a plain circle), and full dark-mode legibility including the modal sheet. Ran the existing Playwright functional regression (revisit CRUD, service+report+medal flow, language switch) with zero console errors — this was a pure-CSS change so functional risk was inherently low, but worth confirming nothing's layout-broken (e.g. no interactive element newly overlapping another after the radius/type changes).
- Follow-ups: none — this is intentionally a lighter "Expressive-flavored" token pass, not a full swap to Google's exact M3 Expressive component specs (which would require the real `@material/web` library per the earlier tradeoff discussion, not chosen here).

## 2026-09-20 (10)
- Request: user testing on their iPhone (Safari) reported not seeing the install button, asked whether it would show in Chrome instead.
- Files changed: `js/install.js` — added `isIOS()` (reuses the same iOS/iPadOS UA + `navigator.platform`/`maxTouchPoints` detection pattern already used in the landing page's `web-install.js`). `js/views/settingsView.js` — `installSectionHTML()` now checks `isIOS()` before falling back to the generic `manualInstallHint` text, showing a new Safari-specific instruction instead. `js/i18n-strings.js` — added `manualInstallHintIOS` (18 languages): "En Safari: toca el botón Compartir (el cuadro con la flecha hacia arriba) y luego elige 'Añadir a pantalla de inicio'" and equivalents.
- Decisions: confirmed for the user that this is expected Safari behavior, not a bug — `beforeinstallprompt` is a Chromium-only API (Chrome/Edge/Android/desktop), Apple has never implemented it in WebKit/Safari, so no in-page JS can make a real "Install" button appear there; the only install path on iOS is the manual Share-sheet flow. Used `fa-share-square` for the icon next to the instruction — initially wrote `fa-arrow-up-from-bracket` (Apple's actual Share-icon glyph) but caught mid-edit that it's a Font Awesome **6** name, same class of mistake as the earlier icon audit; `fa-share-square` (a square-with-up-arrow, the closest FA5.6.3 equivalent) is what's actually loaded by this app.
- Verification: `node --check` passed on both touched JS files; a script confirmed `manualInstallHintIOS` exists in all 18 languages. Playwright with a real iPhone Safari user agent string confirmed the Ajustes install card now renders the Safari-specific text instead of the generic one.
- Follow-ups: ask the user to confirm on their actual iPhone that the new Safari instructions read correctly, and separately confirm that Chrome (on Android or desktop) does show the real "Instalar app" button / bottom banner as expected.

## Template
- Date:
- Request:
- Files changed:
- Decisions:
- Verification:
- Follow-ups:
